{
  "name": "mapizzaioli",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Calculadora de masa para pizza — recetas con biga e hidratación ajustable.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "import:popurri": "node scripts/import-popurri.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource/fraunces": "^5.3.0",
    "@fontsource/inter": "^5.3.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vitest": "^4.1.11"
  }
}
